package SuperBasic;

public class Main {

    public static void main(String[] args) {

        //SuperToCallSuperClassOverriddenAttribute.display();
        //SuperToCallSuperClassOverriddenMethods.display();
        SuperToCallSuperClassDefaultConstructor.display();
        //SuperToCallSuperClassParameterizeConstructor.display();



// Super() Basic:

        // The super keyword in Java is used in subclasses to access superclass members (attributes, constructors and methods).
        // super() is used to call superclass overridden methods, superclass overridden attributes and superclass constructor.

        // super should always be the first statement of the subclass constructor. 


// Uses of super() keyword:
        // To call methods of the superclass that is overridden in the subclass.
        // To access attributes (fields) of the superclass if both superclass and subclass have attributes with the same name.
        // To access the superclass constructor from subclass.

    }
}
