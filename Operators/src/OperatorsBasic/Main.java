package OperatorsBasic;

public class Main {

    public static void main(String[] args) {
        //AssignmentOperator.display();
        //ArithmeticOperators.display();
        //UnaryOperators.display();

        //RelationalOperator.display();
        //LogicalOperator.display();
        //TernaryOperator.display();
        //BitwiseOperator.display();
        //InstanceOf.display();
        InstanceOf.display1();
        //InstanceOf.display2();
    }


// Assignment Operator:
        // =, +=, -=, *=, /=, %=, <<=, >>=, &=, ^=, |=


// Arithmetic Operators:
        // +, -, *, /, %


// Unary Operator:
        // +, -, ++, --, !


// Comparison (Relational) Operators:
        // ==, !=, >, <, >=, <=


// Logical Operator:
        // ||, &&


// instanceof Operator:
        // which compares an object to a specified type or not. return boolean.
                // String test = "asdf";
                // boolean result;
                // result = test instanceof String;
                // System.out.println(result);


// Ternary Operator:
        // ?: or if-then-else statement
                // int februaryDays = 29;
                // String result;
                // result =  (februaryDays == 28) ? "Not a leap year" : "Leap year";
                // System.out.println(result);


// Bitwise and Bit Shift Operators
        // ~ (Complement)
        // << (Left Shift)
        // >> (Right Shift)
        // >>> (Unsigned Right Shift)
        // & (AND)
        // ^ (XOR)
        // | (inclusive OR)

}
